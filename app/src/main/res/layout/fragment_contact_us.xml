<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <Spinner
        android:id="@+id/contact_us_city_spinner"
        android:layout_width="300dp"
        android:layout_height="40dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="5dp"
        android:autofillHints="Choice City1"
        android:background="@drawable/bac_ground"
        android:entries="@array/city1"
        android:prompt="@string/SearchingCity" />


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/contact_us_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/contact_us_city_spinner"
        android:layout_marginTop="10dp" />


</RelativeLayout>